server:
  port: 8084

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hardwarestore
    username: postgres
    password: orion
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html

